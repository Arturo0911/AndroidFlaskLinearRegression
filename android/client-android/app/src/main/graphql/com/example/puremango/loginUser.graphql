mutation loginUser{
    loginUser(username: String, password:String){
        employee {
            credentials
            names
            lastNames
            emailAddress
            departmentName

        }
    }
}