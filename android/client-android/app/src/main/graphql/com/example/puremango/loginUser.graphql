mutation loginUser($username: String!, $password: String!){
    loginUser(username: $username, password:$password){
        employee {
            credentials
            names
            lastNames
            phoneNumber
            emailAddress
            departmentId
            departmentName
            username
            password
        }
    }
}